# Direct dependencies, pinned exactly. Transitive packages are resolved dynamically,
# so this is not a complete lock; `pip freeze` records a working environment snapshot.
#
# Installation:
#   py -3.12 -m venv .venv
#   .venv\Scripts\python.exe -m pip install -r requirements.txt

sdc11073==3.0.0
PySide6==6.11.2
