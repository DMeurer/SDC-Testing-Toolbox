# Direct dependencies, pinned exactly. Transitive packages follow from these;
# use `pip freeze` for a full snapshot of a working environment.
#
# Installation:
#   py -3.12 -m venv .venv
#   .venv\Scripts\python.exe -m pip install -r requirements.txt

sdc11073==3.0.0
PySide6==6.11.2
